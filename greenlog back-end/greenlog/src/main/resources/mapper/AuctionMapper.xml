<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.AuctionMapper">
	<insert id="insert">
		insert into auction (auction_seller , auction_buyer, auction_amount , auction_mall_key) 
		value (#{auction_seller},#{auction_buyer},#{auction_amount}, #{auction_mall_key})
	</insert>
	
	<select id="userAList" resultType="hashmap">
		select *, date_format(Auction_regDate,'%Y.%m.%d %T') as fmtdate
		from auction 
		join mall on auction_mall_key=mall_key 
		where auction_seller=#{uid} or auction_buyer=#{uid} 
		order by auction_regDate desc limit #{start}, #{size}
	</select>
	
	<select id="total" resultType="int">
		select count(*) from auction where auction_seller=#{uid} or auction_seller=#{uid}
	</select>
	
</mapper>